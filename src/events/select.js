// Select option capture is handled by fill.js (change event on SELECT elements).
// This module exists for explicit programmatic select tracking if needed.

export function initSelectCapture() {
  // No-op — select_option events are emitted from fill.js change handler
}
