// Checkbox/radio capture is handled by fill.js (change event on checkbox/radio inputs).
// This module exists for explicit programmatic check tracking if needed.

export function initCheckCapture() {
  // No-op — check events are emitted from fill.js change handler
}
